# 📝 AKP Notes(SQLite) – Event & Reminder App
แอปพลิเคชันจัดการกิจกรรมและบันทึกช่วยเตือนความจำแบบ Offline ที่ใช้สถาปัตยกรรม Flutter + SQLite + Provider ตามโจทย์ปฏิบัติการ Lab 11



---

## 📁 โครงสร้างโปรเจกต์ (Project Structure)

```
lib/
  data/
    db/            # ไฟล์จัดการ SQLite (DBHelper) 
    models/        # Data Models (Event, Category) 
  ui/
    state/         # การจัดการ State ด้วย Provider 
    screens/       # หน้าจอหลัก (Home, Add, Detail) 
```

---

## 🔍 Code walkthrough

### `main.dart`
- เป็นไฟล์เริ่มต้นของแอป
- ใช้ ChangeNotifierProvider เพื่อประกาศ EventProvider ให้สามารถใช้งานได้ทั่วทั้งแอป
- กำหนดหน้าแรก (home) เป็น HomeScreen เพื่อแสดงรายการบันทึก
```void main() {
  runApp(
    ChangeNotifierProvider(
      create: (context) => EventProvider(),
      child: const MyApp(),
    ),
  );
}
```

---

### 🧠 ส่วนของ Models
- **`category_model.dart`**: กำหนดโครงสร้างประเภทกิจกรรม เช่น ชื่อประเภท, สีแท็บ (Hex String), และไอคอน
- **`event_model.dart`**: กำหนดข้อมูลกิจกรรม ประกอบด้วย ชื่อ, รายละเอียด, วันที่, เวลาเริ่ม-สิ้นสุด, สถานะ และระดับความสำคัญ

### 📦 ส่วนของ Providers

- **`EventProvider`**: ทำหน้าที่เป็นตัวกลางระหว่าง UI และ SQLite
- ใช้เมธอด **`fetchAndSetEvents()`**: จัดการตะกร้าสินค้า โดยเก็บข้อมูลสินค้าและจำนวน พร้อมเมธอด `addItem()`, `removeItem()` พื่อดึงข้อมูลและแจ้งเตือนหน้าจอให้วาดใหม่ด้วย `notifyListeners()`
- จัดการคำสั่ง CRUD เช่น `addEvent`, `updateStatus` และการกรองข้อมูล (Filter)
### ✨ ส่วนของ Database (SQLite)
- **`db_helper.dart`**: ช้ Package sqflite และ path เพื่อจัดการฐานข้อมูลภายในเครื่อง
- สร้างตาราง categories และ events พร้อมความสัมพันธ์แบบ Foreign Ke
- มีระบบ Seed Data เพื่อเพิ่มประเภทกิจกรรมเริ่มต้น 3 ประเภทเมื่อรันแอปครั้งแรก

### 🖥 ส่วนของหน้าจอ UI (Screens)
- **`HomeScreen`**: แสดงรายการกิจกรรมทั้งหมดแบบ List พร้อม Badge สีตามประเภท และ Chip แสดงสถานะ

- **`AddEventScreen`**: ฟอร์มสำหรับเพิ่มกิจกรรม พร้อมระบบ Validation ตรวจสอบว่า เวลาสิ้นสุดต้องมากกว่าเวลาเริ่ม

- **`EventDetailScreen`**: แสดงรายละเอียดทั้งหมดและมีปุ่มสำหรับเปลี่ยนสถานะกิจกรรม (เช่น Pending -> Completed)

---

## 💡 ผลลัพธ์ของโปรแกรม
- <img width="522" height="1108" alt="image" src="https://github.com/user-attachments/assets/cbd352b6-8a70-4621-ac4d-7e3ec33e7cb7" />
- <img width="514" height="638" alt="image" src="https://github.com/user-attachments/assets/eddf9c3f-fe85-4d01-bf02-e286ee7935a1" />
- <img width="500" height="702" alt="image" src="https://github.com/user-attachments/assets/ccdca769-5c9a-4abb-82b2-cfb05c727bfa" />




---

