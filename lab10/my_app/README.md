# 🧩 my_app – โปรเจกต์ Flutter Lab

แอปพลิเคชัน Flutter แบบหลายหน้าจอ (Multi-screen) ที่สาธิตการใช้งาน **Models**, **Providers**, **HTTP Services** และการจัดการเส้นทาง (Navigation)

เอกสารฉบับนี้ครอบคลุมหัวใจสำคัญดังนี้:
1. ภาพรวมของโปรเจกต์
2. โครงสร้างโฟลเดอร์และไฟล์ที่สำคัญ
3. อธิบายการทำงานของโค้ดทีละขั้นตอน
4. คำแนะนำในการเริ่มต้นใช้งาน

---

## 📁 โครงสร้างโปรเจกต์ (Project Structure)

```
lib/
  main.dart
  models/
    product_model.dart
    user_model.dart
  providers/
    cart_provider.dart
    product_provider.dart
    user_provider.dart
  screens/
    cart_screen.dart
    login_screen.dart
    product_detail_screen.dart
    product_list_screen.dart
    user_form_screen.dart
    user_list_screen.dart
  services/
    user_api_service.dart
```

---

## 🔍 Code walkthrough

### `main.dart`
- เป็นจุดเริ่มต้นหลักในการรันแอปพลิเคชัน
- ตั้งค่า `MultiProvider` เพื่อลงทะเบียน Provider ทั้งหมด (Products, Cart, Users) ให้สามารถเรียกใช้ข้อมูลร่วมกันได้ทุกหน้าจอ
- กำหนดหน้าแรก (`home`) เป็น `ProductListScreen` และใช้ Named Routes ในการจัดการการนำทาง
```dart
void main() {
  runApp(
    MultiProvider(
      providers: [
        ChangeNotifierProvider(create: (_) => ProductProvider()),
        ChangeNotifierProvider(create: (_) => CartProvider()),
        ChangeNotifierProvider(create: (_) => UserProvider()),
      ],
      child: MyApp(),
    ),
  );
}
```

---

### 🧠 ส่วนของ Models
- **`product_model.dart`**: กำหนดคลาส `Product` พร้อมฟิลด์ข้อมูลพื้นฐาน เช่น `id`, `title`, `price`, `description` รวมถึงการทำ JSON Decoding เพื่อรับข้อมูลจาก API
- **`user_model.dart`**: แทนโครงสร้างข้อมูลผู้ใช้ (id, name, email) พร้อมตัวช่วย `fromJson`/`toJson` สำหรับการจัดการข้อมูล

### 📦 ส่วนของ Providers
Provider ทุกตัวจะสืบทอดมาจาก `ChangeNotifier` เพื่อจัดการสถานะและแจ้งเตือน UI เมื่อข้อมูลมีการเปลี่ยนแปลง
- **`ProductProvider`**: จัดการรายการสินค้าและการดึงข้อมูล (Fetch) จาก API
- **`CartProvider`**: จัดการตะกร้าสินค้า โดยเก็บข้อมูลสินค้าและจำนวน พร้อมเมธอด `addItem()`, `removeItem()` และการคำนวณราคารวม
- **`UserProvider`**: จัดการข้อมูลผู้ใช้และประสานงานกับ `UserApiService` เพื่อทำ CRUD (Create, Read, Update, Delete)

### ✨ ส่วนของ Services
- **`user_api_service.dart`**: คลาสสำหรับส่ง HTTP Requests (GET, POST, PUT, DELETE) ไปยัง Backend ทำหน้าที่รับ-ส่งข้อมูล JSON และแปลงเป็น Model

### 🖥 ส่วนของหน้าจอ UI (Screens)
- **`ProductListScreen`**: แสดงรายการสินค้าทั้งหมดในรูปแบบ Grid/List โดยใช้ `Consumer<ProductProvider>`
- **`ProductDetailScreen`**: แสดงรายละเอียดสินค้า และมีปุ่มเพิ่มสินค้าลงตะกร้าผ่าน `CartProvider`
- **`CartScreen`**: แสดงรายการสินค้าที่เลือก ราคารวม และจัดการลบสินค้า
- **`LoginScreen`**: หน้าจอรับข้อมูลเข้าสู่ระบบ
- **`UserListScreen` & `UserFormScreen`**: ส่วนจัดการข้อมูลผู้ใช้และฟอร์มกรอกข้อมูล
---

## 🚀 เริ่มต้นใช้งาน (Getting Started)

1. **Install Flutter** – follow [flutter.dev](https://flutter.dev/docs/get-started/install).  
2. **Get dependencies**  
   ```bash
   flutter pub get
   ```
3. **Run the app**  
   ```bash
   flutter run
   ```
   Choose an emulator or physical device.

---

## 💡 ผลลัพธ์ของโปรแกรม


---

